export default interface Adventure {
  title: string;
  country: string;
  length_of_stay: number;
  img: any;
}

export interface ActiveAdv {
  title: string;
  country: string;
  length_of_stay: number;
  img: any;
  id: number;
}
